# Basic configuration
app_name: DownloaderBot

app:
  name: "Downloader Bot"
  version: "1.0.0"

server:
  host: "0.0.0.0"
  port: 8000

telegram:
  api_id: 123456
  api_hash: "your_api_hash"
  token: "${TELEGRAM_BOT_TOKEN}"

platforms:
  youtube: true
  tiktok: true
  instagram: true